# --- Oaksy backend (FastAPI) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
PyJWT==2.10.1
python-multipart==0.0.20
anthropic==0.42.0
httpx==0.28.1

# Postgres driver (psycopg 3). Harmless for SQLite dev; required for a
# Postgres DATABASE_URL in production.
psycopg[binary]==3.2.3

# --- Data pipeline (scripts/pull_nfl.py) lives in requirements-pipeline.txt
#     (pandas is heavy and only needed to run the pipeline, not the app). ---
