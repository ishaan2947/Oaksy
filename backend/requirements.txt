# --- Oaksy backend (FastAPI) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
PyJWT==2.10.1
python-multipart==0.0.20
anthropic==0.42.0
httpx==0.28.1

# --- Data pipeline (scripts/pull_nfl.py) ---
# pandas reads nflverse play-by-play CSV.gz directly; only needed to run the pipeline.
pandas==2.2.3

# --- Postgres driver (optional; SQLite is the default dev DB) ---
# psycopg[binary]==3.2.3
